name: Run k6 Browser Test (with Browserless)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  k6-test:
    runs-on: ubuntu-latest

    services:
      browserless:
        image: ghcr.io/browserless/chromium
        ports:
          - 3000:3000
        env:
          TOKEN: test123

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install k6
        run: |
          sudo apt-get update
          sudo apt-get install -y gpg
          curl -s https://dl.k6.io/key.gpg | sudo gpg --dearmor -o /usr/share/keyrings/k6-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install -y k6

      - name: Wait for Browserless service
        run: sleep 5

      - name: Run k6 test (connected to Browserless)
        run: k6 run --compatibility-mode=base k6-test.js

      - name: Upload screenshot artifact
        uses: actions/upload-artifact@v4
        with:
          name: saucedemo-screenshot
          path: saucedemo-dashboard.png
